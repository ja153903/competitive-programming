#include <bits/stdc++.h>

using namespace std;

int main() {
    set<int> s;

    // set<int>::iterator it = s.begin();
    // an easier way to write this is
    auto it = s.begin(); // this will be checked during compilation.
    return 0;
}
