#include <bits/stdc++.h>

using namespace std;

// we can do the following to shorten type names

typedef long long ll; // we can alias long long this way
typedef vector<int> vi;

#define REP(i, a, b) for (int i = a; i <= b; i++)


